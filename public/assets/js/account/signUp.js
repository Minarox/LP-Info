/*
getId('signUp').addEventListener('click', function (event) {

    // event.preventDefault();
    const formData = new FormData(getId('signUp_form'));
    postData(formData, '/ajax/signUpSystem').catch(error => {
        console.log('Error :', error)
    })
})*/
